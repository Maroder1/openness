CREATE TABLE
    Dll_Path (
        Tia_Version INTEGER PRIMARY KEY,
        path VARCHAR(200)
    );

CREATE TABLE
    CPU_List (
        mlfb VARCHAR(50) PRIMARY KEY,
        description VARCHAR(500)
    );